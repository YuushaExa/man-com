{
  "name": "manga-downloader",
  "version": "1.0.0",
  "description": "Download and optimize manga images via GitHub Actions",
  "private": true,
  "type": "module",
  "scripts": {
    "download": "node basic.js",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "@squoosh/cli": "0.7.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
